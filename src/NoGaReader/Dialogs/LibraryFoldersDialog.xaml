<Window x:Class="NoGaReader.Dialogs.LibraryFoldersDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="管理书库文件夹"
        Width="820" Height="590"
        MinWidth="680" MinHeight="500"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        PreviewKeyDown="Window_PreviewKeyDown"
        AutomationProperties.Name="书库文件夹管理">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="管理书库文件夹" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Margin="0,6,0,0"
                       Text="调整扫描范围，或将不再需要的文件夹标记为移除。应用前不会更改书库。"
                       Foreground="{DynamicResource MutedTextBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,18,0,0" Padding="8"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="{StaticResource RadiusMd}">
            <Grid>
                <ListBox x:Name="FolderList"
                         SelectionChanged="FolderList_SelectionChanged"
                         AutomationProperties.Name="书库文件夹列表">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Setter Property="AutomationProperties.Name" Value="{Binding AutomationName}"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid MinHeight="72"
                                  AutomationProperties.Name="{Binding AutomationName}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="190"/>
                                    <ColumnDefinition Width="108"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Margin="9,0,0,0" Text="待移除"
                                                   FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource DangerBrush}"
                                                   Visibility="{Binding RemovalVisibility}"/>
                                    </StackPanel>
                                    <TextBlock Margin="0,5,14,0" Text="{Binding Path}"
                                               Style="{StaticResource CaptionTextStyle}" TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Path}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding IncludeSubfoldersLabel}" Style="{StaticResource CaptionTextStyle}"
                                               Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Margin="0,5,0,0" Text="{Binding LastScannedLabel}"
                                               Style="{StaticResource CaptionTextStyle}"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           Text="{Binding PathStatusLabel}"
                                           AutomationProperties.Name="{Binding PathStatusLabel}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding PathExists}" Value="False">
                                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel x:Name="EmptyState" Visibility="Collapsed"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            AutomationProperties.Name="暂无书库文件夹">
                    <TextBlock Text="暂无书库文件夹" FontSize="{StaticResource FontSizeTitle}" FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Margin="0,6,0,0" Text="请先从书库添加一个文件夹"
                               Foreground="{DynamicResource MutedTextBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border x:Name="SelectionPanel" Grid.Row="2" Margin="0,14,0,0" Padding="16,14"
                Background="{DynamicResource SurfaceAltBrush}" CornerRadius="{StaticResource RadiusMd}"
                DataContext="{Binding ElementName=FolderList, Path=SelectedItem}"
                AutomationProperties.Name="所选文件夹设置">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Margin="0,4,16,0" Text="{Binding Path}"
                               Style="{StaticResource CaptionTextStyle}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <CheckBox Grid.Column="1" Margin="18,0" VerticalAlignment="Center"
                          Content="包含子文件夹"
                          IsChecked="{Binding IncludeSubfolders, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          IsEnabled="{Binding IsEditable}"
                          AutomationProperties.Name="包含子文件夹"/>

                <Button Grid.Column="2" Width="104" VerticalAlignment="Center"
                        Content="{Binding RemovalActionLabel}"
                        Click="ToggleRemovalButton_Click"
                        AutomationProperties.Name="标记或撤销移除所选文件夹"/>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,18,0,0">
            <TextBlock VerticalAlignment="Center" Style="{StaticResource CaptionTextStyle}"
                       Text="Ctrl+Enter 应用 · Esc 取消"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Width="86" Margin="0,0,8,0"
                        Click="CancelButton_Click"
                        AutomationProperties.Name="取消书库文件夹更改"/>
                <Button x:Name="ApplyButton" Content="应用" Width="92"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="ApplyButton_Click"
                        AutomationProperties.Name="应用书库文件夹更改"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
